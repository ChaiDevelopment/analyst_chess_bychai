# Base URL of the Chess Review backend API.
# Leave unset in development - Vite proxies /api to http://localhost:8000 (see vite.config.ts).
# Set explicitly when the frontend and backend are deployed separately.
VITE_API_URL=/api
